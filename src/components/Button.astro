---
export interface Props {
  title: string;
  state: string;
}

const { title, state } = Astro.props;
---

<button class={"btn_" + state} >{title}</button>

<style>
  button {
    background: rgb(49, 100, 255);
    color: white;
    padding: 0.8em 1.6em;
    border: 0;
    font-weight: 800;
    border-radius: 0.1rem;
  }
</style>
